# DeepSeek Application Agent Runtime 0.0 — Release Record

Release artifact per the design freeze (§12/§13). Records the source commit,
sanitized live-smoke evidence, and the tag proposal. Live results are captured
from explicit, opt-in paid runs against real DeepSeek `deepseek-v4-flash`; they
are not part of default CI.

## Source

- Branch: `codex/runtime-0.0-finalize`
- Design: `../specs/2026-07-12-deepseek-application-agent-runtime-0.0-design.md`
- Capabilities & gate evidence: `2026-07-12-runtime-0.0-capabilities.md`
- Cache baseline: `2026-07-12-deepseek-cache-baseline.md`

## Offline suite (deterministic, no network)

Verified with nextest (`RUST_MIN_STACK=8388608`, provider env vars unset):

- `codex-api` Chat Completions SSE parser: text, reasoning+text, reasoning+tool,
  streamed tool-arg accumulation, multiple tool calls, usage + cache parsing,
  malformed-chunk skip, transport error, idle timeout — all pass.
- `codex-core` `suite::deepseek_chat`: text turn, reasoning+text, tool
  call→continuation, mid-turn cancellation, auto-compact-then-continue, and
  persist→resume→continue over `wire_api = "chat_completions"` — all pass.
- `codex-core` `chat_completions_observability` + `client::tests`: request-prefix
  fingerprint stability, thinking-effort mapping, observation-does-not-mutate —
  all pass.
- `codex-models-manager`: 850K compact pin for `deepseek-v4-flash` /
  `deepseek-v4-pro` — pass.
- `app-server` `suite::v2::deepseek_chat`: initialize → thread start → turn →
  streamed completion over a chat_completions provider — pass.

## Sanitized live smoke (paid, opt-in)

Model `deepseek-v4-flash`, `wire_api = "chat_completions"`, isolated
`CODEX_HOME`, run via `codex exec --json`. No secrets or private prompt/tool
content appear below.

### Text + usage

```json
{"type":"item.completed","item":{"id":"item_0","type":"agent_message","text":"ACK"}}
{"type":"turn.completed","usage":{"input_tokens":7915,"cached_input_tokens":2304,"output_tokens":73,"reasoning_output_tokens":0}}
```

Confirms: a full turn completes on the DeepSeek chat path, the assistant message
lands, and provider-reported usage — including nonzero `cached_input_tokens` —
reaches the CLI's public JSON output with no new flag.

### Prompt-cache steady state

See `2026-07-12-deepseek-cache-baseline.md`: three reproducible warm-cache
steady-state runs on the `normal` profile show ~98% harness hit rate, with
`system_changed` / `tools_changed` / `history_rewritten` all false and a
`WithinTolerance` diagnosis — no evidence to change prompt/tool/history
structure.

### Tool call and continuation

Covered deterministically offline (`chat_completions_tool_call_then_continuation`
and `chat_completions_persist_resume_continue`), which assert the tool result is
carried back into the next chat request and is not re-executed on resume. A live
tool smoke can be run on demand with a `danger-full-access` sandbox; it is not
required for the gate because the offline coverage exercises the same path.

## Tag proposal

Proposed tag once merged to `main`:

```
runtime-0.0
```

Recommended annotated-tag message:

```
DeepSeek Application Agent Runtime 0.0

Single-tenant DeepSeek (deepseek-v4-flash) agent runtime over Chat Completions.
Raw/effective/compact context = 1,000,000 / 950,000 / 850,000. Offline gate green
on text/reasoning/tools/usage/cache/compact/resume/app-server; live text smoke and
cache baseline captured. See docs/superpowers/results/2026-07-12-runtime-0.0-*.
```

## Post-0.0 change policy

Per §14: accept fixes for DeepSeek, context correctness, tools, compaction,
persistence, app-server lifecycle, security, and data loss; require a Runtime
0.0 regression test for every supported-path behavior change; do not merge
upstream wholesale, add other-vendor branches, start feature-gated dependency
cutting, add enterprise multi-tenancy, or change the app-server protocol without
an application requirement.
